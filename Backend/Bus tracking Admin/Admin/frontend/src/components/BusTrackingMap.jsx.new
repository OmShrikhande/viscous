import React from 'react';
import BusTrackingMap from './BusTracking';

// This is a wrapper component that maintains backward compatibility
// It uses the modular implementation from the BusTracking folder
const BusTrackingMapWrapper = (props) => {
  return <BusTrackingMap {...props} />;
};

export default BusTrackingMapWrapper;